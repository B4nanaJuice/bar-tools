services:
  web:
    build: .
    container_name: web

    ports:
      - "80:8000"

    env_file: ".env"

  db:
    image: postgres:18
    container_name: db

    env_file: ".env"

    volumes:
      - "postgres_data:/var/lib/postgresql"

volumes:
  postgres_data: